[access]
	inheritFrom = Public-Plugins
[project]
	description = A plugin which allows projects to be renamed from Gerrit via an SSH command
[access "refs/heads"]
	exclusiveGroupPermissions = label-Verified labelAs-Verified
	owner = group plugins-rename-project
